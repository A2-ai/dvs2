# `dvs revert`

## CLI

The option to return `--json` must be present.

## R

Signature:

```r
dvs_revert <- function(
  commit_sha = integer(),
  date = NULL,
  before = NULL, # date | duration
)
```


