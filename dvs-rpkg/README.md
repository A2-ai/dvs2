# `dvs`

## Installation

```r
remotes::install_github("a2-ai/dvs2/dvs-rpkg")
```

### `rv`

In `rproject.toml` add the following: 

```toml
dependencies = [
    {name = "dvs", git = "https://github.com/a2-ai/dvs2", directory = "dvs-rpkg", branch = "main"}
]
```

