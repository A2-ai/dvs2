[package]
name = "dvs-rpkg"
version = "0.3.0"
edition = "2024"
rust-version = '1.85'
publish = false

# Make this a standalone workspace (prevents inheriting from parent)
[workspace]

[lib]
name = "dvs"
path = "lib.rs"
crate-type = ["staticlib"]

[features]
default = []
nonapi = ["miniextendr-api/nonapi"]
connections = ["miniextendr-api/connections"]

[dependencies]
miniextendr-api = { version = "*", features = ["serde"] , path = "../../vendor/miniextendr-api-0.1.0" }
dvs = { version = "*", path = "../../vendor/dvs-0.3.0" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"

[build-dependencies]
miniextendr-lint = { version = "*" , path = "../../vendor/miniextendr-lint-0.1.0" }

[profile.dev]
codegen-units = 1

[profile.release]
codegen-units = 1

[patch]

[patch.crates-io]
dvs = { path = "../../vendor/dvs-0.3.0" }
miniextendr-api = { path = "../../vendor/miniextendr-api-0.1.0" }
miniextendr-lint = { path = "../../vendor/miniextendr-lint-0.1.0" }
miniextendr-macros = { path = "../../vendor/miniextendr-macros-0.1.0" }
