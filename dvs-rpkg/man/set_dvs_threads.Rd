% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dvs-options.R
\name{set_dvs_threads}
\alias{set_dvs_threads}
\title{Set the number of threads used by DVS operations}
\usage{
set_dvs_threads(threads)
}
\arguments{
\item{threads}{Integer number of threads to use. Set to \code{NULL} to revert to
the default (automatic detection).}
}
\value{
The previous value of \code{dvs.num_threads} (invisibly).
}
\description{
Controls the thread pool size for parallel file operations (add, get, status).
The value is stored as the \code{dvs.num_threads} option and synced to the
Rust backend before each operation.
}
\details{
Can also be set via \code{.Rprofile} or temporarily with \code{\link[withr:with_options]{withr::with_options()}}.
}
\examples{
\dontrun{
set_dvs_threads(4)

# Temporarily use 2 threads
withr::with_options(list(dvs.num_threads = 2), {
  dvs_add(files = "big_data.csv")
})
}

}
