% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dvs.R
\name{dvs_merge_repo}
\alias{dvs_merge_repo}
\title{Merge Repository}
\usage{
dvs_merge_repo(
  source,
  prefix = NULL,
  conflict_mode = "abort",
  verify = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{source}{Character string specifying path to source repository.}

\item{prefix}{Optional character string specifying subdirectory prefix
for imported files.}

\item{conflict_mode}{Character string specifying how to handle path conflicts:
\itemize{
\item "abort": Abort if any path exists in both repos (default)
\item "skip": Keep destination file, skip source
\item "overwrite": Replace destination with source
}}

\item{verify}{Logical indicating whether to verify object hashes during copy.
Default is FALSE.}

\item{dry_run}{Logical indicating whether to show what would be merged without
making changes. Default is FALSE.}
}
\value{
A list with merge results (files_merged, files_skipped, objects_copied,
objects_existed, conflicts, merged_paths).
}
\description{
Merge tracked files, metadata, and objects from a source DVS repository
into the current (destination) repository.
}
\examples{
\dontrun{
# Merge another repository
dvs_merge_repo("../other-project")

# Merge with prefix and skip conflicts
dvs_merge_repo("../other-project", prefix = "imported", conflict_mode = "skip")

# Dry run to see what would be merged
dvs_merge_repo("../other-project", dry_run = TRUE)
}
}
